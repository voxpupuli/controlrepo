# frozen_string_literal: true

# Managed by modulesync - DO NOT EDIT
# https://voxpupuli.org/docs/updating-files-managed-with-modulesync/

source ENV['GEM_SOURCE'] || 'https://rubygems.org'

group :test do
  gem 'voxpupuli-test', '~> 14.0', require: false
  gem 'puppet_metadata', '~> 6.0', require: false
end

group :system_tests do
  gem 'voxpupuli-acceptance', '~> 4.2', require: false
end

gem 'openfact', ENV['FACTER_GEM_VERSION'], require: false, groups: [:test]
gem 'rake', require: false

puppetversion = ENV['PUPPET_GEM_VERSION'] || '>= 8.19'
gem 'metadata_json_deps', require: false
gem 'openvox', puppetversion, require: false, groups: [:test]
gem 'syslog'
# vim: syntax=ruby
